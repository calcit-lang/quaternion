# Upgrade to Calcit 0.14.7 / 升级到 Calcit 0.14.7

- Align the module, compiler, and JavaScript runtime with Calcit 0.14.7 and prepare quaternion 0.2.6.
- Add complete schemas for all 48 definitions, migrate 31 deprecated `tag-match` calls, and keep `v-scale` polymorphic with an explicit input/output type relation instead of Dynamic.
- Require a first value for `c+`, `v-`, and `q-`; their former zero-argument path produced `nil` and could not satisfy the public numeric contracts.
- Run the public library and native/JavaScript behavior gates under the zero-debt strict type checker.
- 将模块、编译器与 JavaScript runtime 对齐到 Calcit 0.14.7，并准备 quaternion 0.2.6。
- 为全部 48 个定义补全 schema，迁移 31 处弃用的 `tag-match`，并用显式输入/输出类型关系表达 `v-scale` 的多态，而不是 Dynamic。
- `c+`、`v-` 与 `q-` 现在要求至少一个输入；旧的零参数路径会返回 `nil`，无法满足公开数值契约。
- 在零债务严格类型检查器下运行公开类库以及 native/JavaScript 行为门禁。
